void main(List<String> args) {
  /**
   * Symbol -> tipe data yang bisa digunakan sebagai constant
   * unutk membuat symbol. bisa menggunakan tanda #, atau jika butuh symbol dengan nama yang mengandung spasi, bisa menggunakan symbol("text")
   */

  /// create Symbol
  Symbol symbol1 = Symbol('Yusman 13020200252');
  var symbol2 = #Yusman;

  print(symbol1);
  print(symbol2);
}
